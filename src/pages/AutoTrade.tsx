import { useEffect, useState } from "react"; import { Card } from "../components/Card"; import { autoTradeApi } from "../services/api"; import { Play, Square, Activity, XCircle, Target, Shield, Zap, BarChart3, Settings } from "lucide-react"; interface Position { id: string; optionSymbol: string; quantity: number; entryPrice: number; currentSL: number; target: number; pnl: number; status: string; underlying: string; } interface Signal { type: string; entryPrice: number; stopLoss: number; target: number; timestamp: number; underlying: string; status: string; } interface AutoTradeStatus { isRunning: boolean; marketStatus: string; todayTrades: number; maxTrades: number; openPositions: Position[]; activeAlerts: Record<string, any>; latestData: Record<string, any>; recentSignals: Signal[]; capital: number; riskPercent: number; paperTrading: boolean; emergencyStop: boolean; positionSizingMode?: string; fixedLots?: number; dailyPnL?: string; consecutiveLosses?: number; } export function AutoTrade() { const [status, setStatus] = useState<AutoTradeStatus | null>(null); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [showConfig, setShowConfig] = useState(false); const [configForm, setConfigForm] = useState({ riskPercent: 0.5, maxTradesPerDay: 10, paperTrading: false, positionSizingMode: "RISK", fixedLots: 1 }); const fetchStatus = async () => { try { const data = await autoTradeApi.getStatus(); setStatus(data); setError(""); } catch (err: any) { setError(err.message || "Failed to fetch status"); } }; useEffect(() => { fetchStatus(); const interval = setInterval(() => { fetchStatus(); }, 5000); return () => clearInterval(interval); }, []); const handleStart = async () => { setLoading(true); setError(""); try { await autoTradeApi.start(); await fetchStatus(); } catch (err: any) { setError(err.message || "Failed to start"); } finally { setLoading(false); } }; const handleStop = async () => { setLoading(true); setError(""); try { await autoTradeApi.stop(); await fetchStatus(); } catch (err: any) { setError(err.message || "Failed to stop"); } finally { setLoading(false); } }; const handleUpdateConfig = async () => { setLoading(true); try { await autoTradeApi.updateConfig(configForm); await fetchStatus(); setShowConfig(false); } catch (err: any) { setError(err.message || "Failed to update config"); } finally { setLoading(false); } }; const isRunning = status?.isRunning || false; return ( <div> <div className="flex items-center justify-between"> <div> <h1 className="text-3xl font-semibold tracking-tight text-white">Auto Trader</h1> <p className="mt-2 text-sm text-zinc-500">Automated 5 EMA strategy by Subhasish Pani</p> </div> <div className="flex items-center gap-3"> {isRunning ? ( <span className="flex items-center gap-1.5 rounded-full bg-lime-400/10 px-3 py-1.5 text-xs font-medium text-lime-300"> <Activity size={14} className="animate-pulse" /> Running </span> ) : ( <span className="flex items-center gap-1.5 rounded-full bg-zinc-800 px-3 py-1.5 text-xs font-medium text-zinc-400"> <XCircle size={14} /> Stopped </span> )} </div> </div> <Card className="mt-6"> <div className="flex items-center justify-between"> <div> <h2 className="text-lg font-medium text-white">Trading Bot Control</h2> <p className="mt-1 text-sm text-zinc-500">{isRunning ? "Bot is actively scanning for 5 EMA setups" : "Start the bot to begin automated trading"}</p> </div> <div className="flex gap-3"> <button onClick={() => setShowConfig(!showConfig)} className="flex items-center gap-2 rounded-xl bg-zinc-800 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-zinc-700"> <Settings size={16} /> Config </button> {!isRunning ? ( <button onClick={handleStart} disabled={loading} className="flex items-center gap-2 rounded-xl bg-lime-400 px-5 py-2.5 font-semibold text-zinc-950 transition hover:bg-lime-300 disabled:opacity-50"> <Play size={18} /> {loading ? "Starting..." : "Start Bot"} </button> ) : ( <button onClick={handleStop} disabled={loading} className="flex items-center gap-2 rounded-xl bg-rose-500 px-5 py-2.5 font-semibold text-white transition hover:bg-rose-400 disabled:opacity-50"> <Square size={18} /> {loading ? "Stopping..." : "Stop Bot"} </button> )} </div> </div> {error && <p className="mt-4 rounded-xl border border-rose-400/20 bg-rose-400/10 p-3 text-sm text-rose-300">{error}</p>} </Card> {showConfig && ( <Card className="mt-4 border-amber-400/20"> <div className="flex items-center justify-between mb-4"> <h3 className="text-sm font-medium text-amber-300 flex items-center gap-2"> <Settings size={16} /> Strategy Configuration </h3> <button onClick={handleUpdateConfig} disabled={loading} className="rounded-lg bg-lime-400 px-3 py-1.5 text-xs font-semibold text-zinc-950 hover:bg-lime-300 disabled:opacity-50"> Save Config </button> </div> <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"> <div> <label className="text-xs text-zinc-500">Sizing Mode</label> <select value={configForm.positionSizingMode} onChange={(e) => setConfigForm({...configForm, positionSizingMode: e.target.value})} className="mt-1 w-full rounded-lg border border-zinc-800 bg-zinc-950 px-3 py-2 text-sm text-white"> <option value="RISK">Risk %</option> <option value="LOTS">Fixed Lots</option> </select> </div> {configForm.positionSizingMode === "RISK" ? ( <div> <label className="text-xs text-zinc-500">Risk Per Trade (%)</label> <input type="number" step="0.1" min="0.1" max="5" value={configForm.riskPercent} onChange={(e) => setConfigForm({...configForm, riskPercent: parseFloat(e.target.value)})} className="mt-1 w-full rounded-lg border border-zinc-800 bg-zinc-950 px-3 py-2 text-sm text-white" /> </div> ) : ( <div> <label className="text-xs text-zinc-500">Lots Per Trade</label> <input type="number" min="1" max="100" value={configForm.fixedLots} onChange={(e) => setConfigForm({...configForm, fixedLots: parseInt(e.target.value)})} className="mt-1 w-full rounded-lg border border-zinc-800 bg-zinc-950 px-3 py-2 text-sm text-white" /> </div> )} <div> <label className="text-xs text-zinc-500">Max Trades/Day</label> <input type="number" min="1" max="100" value={configForm.maxTradesPerDay} onChange={(e) => setConfigForm({...configForm, maxTradesPerDay: parseInt(e.target.value)})} className="mt-1 w-full rounded-lg border border-zinc-800 bg-zinc-950 px-3 py-2 text-sm text-white" /> </div> <div className="flex items-center gap-3 pt-5"> <label className="text-xs text-zinc-500">Paper Trading</label> <input type="checkbox" checked={configForm.paperTrading} onChange={(e) => setConfigForm({...configForm, paperTrading: e.target.checked})} className="h-4 w-4 rounded border-zinc-600 bg-zinc-950" /> </div> </div> </Card> )} {status && ( <> <div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"> <Card><div className="flex items-center gap-3"><div className="rounded-lg bg-blue-400/10 p-2 text-blue-300"><BarChart3 size={20} /></div><div><p className="text-xs text-zinc-500">Market Status</p><p className="text-lg font-semibold text-white">{status.marketStatus}</p></div></div></Card> <Card><div className="flex items-center gap-3"><div className="rounded-lg bg-amber-400/10 p-2 text-amber-300"><Zap size={20} /></div><div><p className="text-xs text-zinc-500">Trades Today</p><p className="text-lg font-semibold text-white">{status.todayTrades} / {status.maxTrades}</p></div></div></Card> <Card><div className="flex items-center gap-3"><div className="rounded-lg bg-lime-400/10 p-2 text-lime-300"><Target size={20} /></div><div><p className="text-xs text-zinc-500">Open Positions</p><p className="text-lg font-semibold text-white">{status.openPositions?.length || 0}</p></div></div></Card> <Card><div className="flex items-center gap-3"><div className="rounded-lg bg-purple-400/10 p-2 text-purple-300"><Shield size={20} /></div><div><p className="text-xs text-zinc-500">Capital</p><p className="text-lg font-semibold text-white">₹{(status.capital || 0).toLocaleString()}</p></div></div></Card> </div> <Card className="mt-6"> <h3 className="text-sm font-medium text-white">Strategy Configuration</h3> <div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3"> <div className="rounded-lg border border-zinc-800 bg-zinc-950 p-3"><p className="text-xs text-zinc-500">Sizing Mode</p><p className="text-sm font-medium text-white">{status.positionSizingMode || "RISK"}</p></div> <div className="rounded-lg border border-zinc-800 bg-zinc-950 p-3"><p className="text-xs text-zinc-500">{status.positionSizingMode === "LOTS" ? "Lots/Trade" : "Risk Per Trade"}</p><p className="text-sm font-medium text-white">{status.positionSizingMode === "LOTS" ? (status.fixedLots || 1) + " lots" : (status.riskPercent || 1) + "%"}</p></div> <div className="rounded-lg border border-zinc-800 bg-zinc-950 p-3"><p className="text-xs text-zinc-500">Max Trades/Day</p><p className="text-sm font-medium text-white">{status.maxTrades || 10}</p></div> <div className="rounded-lg border border-zinc-800 bg-zinc-950 p-3"><p className="text-xs text-zinc-500">Paper Trading</p><p className="text-sm font-medium text-white">{status.paperTrading ? "ON" : "OFF"}</p></div> <div className="rounded-lg border border-zinc-800 bg-zinc-950 p-3"><p className="text-xs text-zinc-500">Daily P&L</p><p className={`text-sm font-medium ${parseFloat(status.dailyPnL || '0') >= 0 ? 'text-lime-300' : 'text-rose-300'}`}>₹{status.dailyPnL || 0}</p></div> <div className="rounded-lg border border-zinc-800 bg-zinc-950 p-3"><p className="text-xs text-zinc-500">Emergency Stop</p><p className={`text-sm font-medium ${status.emergencyStop ? 'text-rose-300' : 'text-lime-300'}`}>{status.emergencyStop ? "ACTIVE" : "Ready"}</p></div> </div> </Card> </> )} </div> ); }